

radio.c

when receiving packet:

  when using read irq status and test packet receive
  if transmit is delay 3 seconds, then it receive lots of same packet
  
  when using read dio1 pin
  if transmit is delay 3 seconds, then it only one receive packet
